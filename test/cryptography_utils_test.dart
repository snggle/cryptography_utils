import 'package:flutter_test/flutter_test.dart';

void main() {
  // TODO(dominik): remove this test after creating real tests
  test('Temporary test to allow GitHub runners work correctly', () {
    expect(1, 1);
  });
}
