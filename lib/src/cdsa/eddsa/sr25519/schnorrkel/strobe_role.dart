/// This enum is used to define the role of a participant in a STROBE protocol
/// exchange, particularly in the context of transport or communication protocols.
enum StrobeRole {
  iNone,
  iInitiator,
  iResponder,
}
